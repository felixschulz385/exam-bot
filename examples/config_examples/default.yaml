# Example override file.
# The full default settings now live in ../../config/exam_settings.yaml.
# Use a file like this only when you want to override a few fields.

exam:
  title: "Advanced Health Economics - Final Exam 2025"

selection:
  target_points: 60
